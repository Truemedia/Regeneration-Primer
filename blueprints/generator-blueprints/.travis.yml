language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-blueprints' ]; then cd .. && eval "mv $currentfolder generator-blueprints" && cd generator-blueprints; fi

